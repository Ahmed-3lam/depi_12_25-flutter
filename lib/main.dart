import 'package:flutter/material.dart';

import 'bmi_calc.dart';

void main() {
 
  runApp(const MyApp());
}
























class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: BmiCalc());
  }
}

/// Material App
/// Scaffold
/// Container
/// Text
/// Center
/// Row
/// Column
/// SizedBox
/// CircleAvatar
/// ListView.builder (for loop)
/// ListView.separated (for loop)
/// Expanded
/// Spacer
/// AppBar
/// Icon

